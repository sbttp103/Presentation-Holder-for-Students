<%= simple_form_for([@presentation, @presentation.feedbacks.build], remote: true) do |f| %>

</br>
<div>
  <div class="form-group"> 
    <%= f.input :feedback, label: false %>
  </div>
</div>
</br>
<%= f.button :submit, 'Submit', class:"btn btn-primary" %>

<% end %>